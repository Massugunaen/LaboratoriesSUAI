#include <iostream>
using namespace std;

int main()
{
    int a, a1, b, b1;
    cin >> a >> b >> a1 >> b1; // try to make through tg !
    
    if ((a != a1) && (b != b1)) // a/a1 // b/b1 and through coordinates
    {
        cout << "YES" << endl;
    }
    
    else 
    {
        cout << "NO" << endl;
    }
    return 0;
}
